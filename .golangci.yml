run:
  timeout: 5m
  tests: true

linters:
  disable-all: true
  enable:
    - errcheck
    - gosec
    - staticcheck
    - govet
    - ineffassign
    - unused

issues:
  exclude-dirs:
    - mudmaker
